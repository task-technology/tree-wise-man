import PaymentsList from "@modules/Dashboard Content/Payments pages/Payments List/PaymentsList";

const page = () => {
  return (
    <div>
      <PaymentsList />
    </div>
  );
};

export default page;
