export type UserProfile = {
  user?: {
    name: string;
    email: string;
    bio: string;
    profilePicture: string;
    phoneNumber: string;
    location: string;
  };
  // Add other fields as needed
};
